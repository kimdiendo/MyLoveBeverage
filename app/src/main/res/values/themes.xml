<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyLoveBeverage" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
    </style>
    <!--menu text style. -->
    <style name="menu_text_style" parent="@android:style/TextAppearance.DeviceDefault.Widget.ActionBar.Menu">
        <item name="android:textSize">25dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="Theme.MyLoveBeverage.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MyLoveBeverage.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MyLoveBeverage.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>